import 'package:flutter/material.dart';

// Colores de la Tercia (Esquema Triádico)
const color1 = Color(0xff4a5568);	// Gris Pizarra (Base sólida y profesional)
const color2 = Color(0xffa0aec0);	// Gris Azulado (Elegancia y claridad)
const color3 = Color(0xffcbd5e0);	// Humo Suave (Perfecto para fondos o detalles)

// Colores de soporte para la interfaz
const colorBackground = Color(0xffeaf6f2); // El fondo claro que usas en el login
const colorTextDark = Color(0xff353535);   // Gris oscuro para legibilidad
const colorWhite = Colors.white;

// Credenciales de prueba
const user = 'Victor';
const pwd = '12345';

// Datos del Singleton
const name = 'Victor';
const lastname = 'Perez';
const wallet = 1000;